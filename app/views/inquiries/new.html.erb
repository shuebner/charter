<%= semantic_form_for @inquiry do |i| %>
  <%= i.inputs :first_name, :last_name, :email, :text %>
  <%= i.actions do %>
    <%= i.action :submit %>
    <%= i.action :cancel, url: root_path %>
  <% end %>
<% end %>